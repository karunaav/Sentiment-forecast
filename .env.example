# If you have a FRED API key, put it here (optional)
FRED_API_KEY=

# Use FinBERT (requires PyTorch). If false, fallback to VADER.
USE_FINBERT=true

# MLflow config (optional)
MLFLOW_TRACKING_URI=file:./mlruns
MLFLOW_EXPERIMENT=sentiment-forecast
